import { Routes } from '@angular/router';

import { LoanSimulatorComponent } from './components/loan-simulator/loan-simulator';

export const routes: Routes = [{ path: '', component: LoanSimulatorComponent }];
